# S.M.I.L.E-ing Boys — Starter Next.js Site

## Run locally

```bash
npm install
npm run dev
```

Then open http://localhost:3000

## Where your images are

All portrait assets are in:

`public/images/`

The gallery sources are defined in `app/page.js`.

## Next improvements (easy wins)

- Replace placeholder email `hello@smileingboys.org` with your real inbox.
- Swap placeholder impact stats with your real numbers.
- Add pages: `/programmes`, `/impact`, `/press`, `/donate`.
- Add a real contact form (Formspree/Basin/Resend) + spam protection.
- Add SEO: OpenGraph images, proper titles per page.

